<h1>Listing rules</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Enabled</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @rules.each do |rule| %>
    <tr>
      <td><%= link_to rule.name, event_rule_path(rule) %></td>
      <td><%= link_to 'Destroy', event_rule_path(rule), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br />

<%= link_to 'New Rule', new_event_rule_path %>
